# Сборочный мусор
bin/
obj/

# Готовые архивы выпусков — они живут в GitHub Releases, а не в истории файлов
dist/

# Библиотеки BepInEx и HarmonyX для компиляции. Это чужие сборки, и класть их
# в репозиторий нельзя. Проект берёт их из установленного BepInEx, а если его
# нет — сборка скажет, где взять (см. Forgeplan.csproj).
lib/
BepInExPack_Valheim-*.zip

# Номер выпуска генерируется из VERSION при сборке
src/Version.g.cs

__pycache__/
*.pyc
